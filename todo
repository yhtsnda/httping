- multiple threads! (?)

- koppelen aan iets externs? dus een thread die v/e ander programma eea opvangt en dan meeneemt in de logging? (gps bijv.)

- http 302 volgen?

- o2oo-achtig rapport?

- ping with timestamp:
	ping google.com | while read i ; do echo `date +%s.%N` $i ; done

- kleuren in ncurses mode

- translate gettext

- -A en -J vrijmaken

- if max_x > ...
  - processing time / content-size stats
  - content-size stats

- multiple urls

- help groeperen, dus switches per type
  misschien een show_help(short, long, descr) waarbij descr op vaste positie begint (tenzij pos > max_x) en dan wrapped op max_x (wordwrap if aantal_chars < 5) en dan doorgaat op pos

- SPDY

- first request: show request headers size / data

- cookies uit chrome/firefox/etc cache halen

set args --graph-limit 250 -K -i 0.1 -W --aggregate 3,5,7 -Q belle
